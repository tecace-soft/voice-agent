# TecAce voice agent dashboard configuration (template).
# Copy to .env and fill in per machine — .env is git-ignored and does not travel with `git pull`.
# Vite only exposes vars prefixed with VITE_ to the browser.

# Base URL of transcribe-backend (no trailing slash). It serves sign-in, /transcribe/*, /business/*,
# /calls, /usage/*, /feedback, /api-keys and /demo/* — everything every screen reads, Demo included.
BACKEND_URL=http://localhost:8001

# Nothing else is required. The prospect demo page (/c/<id>) is served by this app, so the links
# the Prospects and Share screens copy and email are this origin plus the path — there is no base
# URL to configure and no way for it to be wrong.
#
# Optional: contact links shown on the public demo page. Defaults are TecAce's.
# VITE_CONTACT_URL=
# VITE_PRICING_URL=
# VITE_CONTACT_EMAIL=
