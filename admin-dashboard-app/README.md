# Admin Dashboard

The web dashboard the **Admin** uses to manage and monitor the system (leads, calls,
bookings, configuration, etc.).

> **Status:** placeholder — directory scaffolding only. Tech stack not chosen yet.

## Role in the workspace

- A frontend application, developed and deployed independently of the other apps.
- Talks to the shared **[`backend-app/`](../backend-app/)** for its data and actions — it
  does not access the voice agent's internals directly.

## Not set up yet

This directory is intentionally a stub. When the stack is chosen (e.g. React + Vite, or
Next.js), scaffold the project here and replace this README with real setup/run docs.

See the workspace [root README](../README.md) for how apps fit together and how to add one.
