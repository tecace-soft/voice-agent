# Backend configuration (template).
# Copy to .env and fill in per machine — .env is git-ignored and does not travel with `git pull`.
#
# Placeholder — no variables defined yet. Add them here as the backend takes shape, e.g.:
# PORT=8000
# DATABASE_URL=
